services:
  myip:
    image: jason5ng32/myip:latest
    ports:
      - "${HOST_IP}:${WEB_HTTP_PORT}:18966"
    restart: always
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          cpus: "${CPUS}"
          memory: "${MEMORY_LIMIT}"
    labels:
      createdBy: "bt_apps"
    networks:
      - baota_net

networks:
  baota_net:
    external: true